import React, { StrictMode } from 'react';
import ReactDOM from 'react-dom'; // ReactDOM library allows interaction with the DOM in the browser
import './index.css'
import App from './App';

ReactDOM.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>,
    document.getElementById('root')
);
